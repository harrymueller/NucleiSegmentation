#include "image.hpp"
#include "io.hpp"

#include <stdio.h>
#include <iostream>
#include <fstream>

namespace BinGenes
{
    void BinGenes::bin_genes(Mat, std::string);
    void empty_array(int*, int);
    void BinGenes::save_genes(std::string, std::string, int*);
}